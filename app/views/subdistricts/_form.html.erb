<%= form_for(@subdistrict) do |f| %>
  <% if @subdistrict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subdistrict.errors.count, "error") %> prohibited this subdistrict from being saved:</h2>

      <ul>
      <% @subdistrict.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Simpan', :class => "button big" %>
  </div>
<% end %>
